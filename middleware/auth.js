// const isLogined = false;

// export default defineNuxtRouteMiddleware((to, from) => {
//   if (to === "/chat" && isLogined) {
//     return navigateTo("/chat");
//   } else return navigateTo("/auth");
// });
